
// Contentful integration removed as per user request to rely on static files.
// This file is kept empty to prevent import errors if referenced elsewhere before full cleanup.
export async function fetchEntries() {
  return null;
}
